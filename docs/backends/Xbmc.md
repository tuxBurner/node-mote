# Kodi/Xbmc backend

## Add a remote

### config.json

Under devices you can add a new kodi backend


```json
"XBMC_LOCAL": {
      "type": "xbmc",
      "config": {
        "host": "localhost",
        "port": "9090"
      }
    }
```